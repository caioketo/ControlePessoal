@model ControlePessoalBE.Models.ProdutoModel

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>ProdutoModel</legend>

        @Html.HiddenFor(model => model.ProdutoId)

        <div class="editor-label">
            @Html.LabelFor(model => model.Descricao)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Descricao)
            @Html.ValidationMessageFor(model => model.Descricao)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Quantidade)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Quantidade)
            @Html.ValidationMessageFor(model => model.Quantidade)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.QuantidadeAviso)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.QuantidadeAviso)
            @Html.ValidationMessageFor(model => model.QuantidadeAviso)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
